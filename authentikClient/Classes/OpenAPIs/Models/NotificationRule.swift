//
// NotificationRule.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** NotificationRule Serializer */
public struct NotificationRule: Codable, JSONEncodable, Hashable {

    public var pk: UUID
    public var name: String
    /** Select which transports should be used to notify the user. If none are selected, the notification will only be shown in the authentik UI. */
    public var transports: [UUID]?
    /** Controls which severity level the created notifications will have.  * `notice` - Notice * `warning` - Warning * `alert` - Alert */
    public var severity: SeverityEnum?
    /** Define which group of users this notification should be sent and shown to. If left empty, Notification won't ben sent. */
    public var group: UUID?
    public var groupObj: Group

    public init(pk: UUID, name: String, transports: [UUID]? = nil, severity: SeverityEnum? = nil, group: UUID? = nil, groupObj: Group) {
        self.pk = pk
        self.name = name
        self.transports = transports
        self.severity = severity
        self.group = group
        self.groupObj = groupObj
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case pk
        case name
        case transports
        case severity
        case group
        case groupObj = "group_obj"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(pk, forKey: .pk)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(transports, forKey: .transports)
        try container.encodeIfPresent(severity, forKey: .severity)
        try container.encodeIfPresent(group, forKey: .group)
        try container.encode(groupObj, forKey: .groupObj)
    }
}

