//
// MobileDeviceResponseRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Response from push sent to phone */
public struct MobileDeviceResponseRequest: Codable, JSONEncodable, Hashable {

    public var txId: UUID
    public var selectedItem: String

    public init(txId: UUID, selectedItem: String) {
        self.txId = txId
        self.selectedItem = selectedItem
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case txId = "tx_id"
        case selectedItem = "selected_item"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(txId, forKey: .txId)
        try container.encode(selectedItem, forKey: .selectedItem)
    }
}

